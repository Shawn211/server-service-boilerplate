# 对接 OpenOcean 的除去交易广播的 swap 流程设计思路

1. 基础数据准备：创建 network 表（手动维护支持的链）、token 表（自动同步 OpenOcean 的代币数据）
2. 基础查询接口：
   - 查支持的网络（`GET /token/networks`，带 redis 缓存）
   - 查支持的token列表（`GET /token/list/:chain`，调用 `/v4/:chain/tokenList`，带 redis 缓存）
3. 询价接口（`POST /token/quote`）：
   - 接收参数（链、卖出/买入代币地址、数量等）
   - 获取指定链的 gas 费用（调用 `/v4/:chain/gasPrice`，带 redis 缓存）
   - 调用 `/v4/:chain/quote`，返回可兑换数量、是否需要 approve 等
4. 处理 approve（如需）：
   - 若询价返回`needApprove=true`，提示用户先完成代币授权（调用对应链的代币合约 approve 方法）
5. 创建交易接口（`POST /token/swap`）：
   - 调用 `/v4/:chain/swap`获取交易体
   - 返回交易体数据

# 多渠道商支持设计
1. 渠道商表设计、渠道商支持 token 表设计
   - 渠道商表提供渠道商 id，token 表提供 token id，都作为渠道商支持 token 表的外键和索引建立
   - 渠道商支持 token 表包含 swap 和 quote 接口的路由数据
2. network 表和 token 表数据初始化的多渠道商支持
   - [pre-networks.ts](./scripts/pre-networks.ts) 优化成针对无 api 获取渠道商支持网络脚本，并支持多渠道商分批请求
   - scripts 目录下新增 network.schedule.ts，针对支持 api 获取渠道商支持网络的定时任务
   - [token.schedule.ts](./schedule/token.schedule.ts) 优化成针对多渠道商定时同步支持网络的支持 token 功能
3. trade 模块的 quote 接口和 swap 接口的多渠道商支持
   - 抽象出渠道商 trade 基类，每个渠道商继承这个基类，实现基类的 quote 和 swap 方法
   - 在 quote 和 swap 接口中，根据渠道商支持 token 表，获取请求对应 token 支持的渠道商配置
   - 基于获取到的渠道商配置，调用对应渠道商派生类的 quote 和 swap 方法，并由渠道商派生类格式化数据，返回结果

# 优化策略
1. 多渠道商询价结果快速有效返回
   - 不同渠道商响应速度不同，可采用 SSE 方案异步直接推送客户端，提升体验
      - 构建服务端 SSE 服务接口（[midway 3.17 内置 SSE 支持](https://midwayjs.org/docs/data_response#sse-%E5%93%8D%E5%BA%94)，但本服务 midway 版本 3.14）
      - 询价接口的多渠道商询价逻辑改造异步多渠道商询价，每个渠道商询价结果通过 SSE 推送给客户端
2. 渠道商支持代币信息庞大且需要与服务维护的对应信息保持一致
   - 定时任务 api 全量获取，hash 对比 redis 记录渠道商支持代币 hash
      - 一致则跳过后续逻辑
      - 不一致进行数据库数据对比，更新 redis hash
   - 数据库数据对比时，分页分批一一对比，减轻数据库压力
